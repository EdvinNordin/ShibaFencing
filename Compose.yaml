version: '3.8'
services:
  client:
    build:
      context: ./Client
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - server

  server:
    build:
      context: ./Server
      dockerfile: Dockerfile
    ports:
      - "8181:8181"